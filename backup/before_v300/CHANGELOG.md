# IOMASTAVKA — дизайн-обновление v200

Заменить файлы: `styles.css`, `app.js`, `index.html`. `server.js` и `package.json` не менялись.
Версия кэша в index.html поднята до `?v=20260920-v200`.

## styles.css (168 КБ → ~66 КБ)
- Полная переработка на токенах: цвет, типографика, радиусы, анимации. Без `!important` (кроме `.hidden`).
- Светлая: soft blue + teal. Тёмная: navy + teal (вместо золотого).
- Убран блокирующий `@import` Google Fonts (Inter не использовался).
- Окна = листы iOS; главная уходит на второй план (нет просвечивания); шапка не меняет высоту.
- Мобильная версия: окна на весь экран, поля 16px, карточки экспедиторов, safe-area, dvh.
- Фоновая сцена: без blur на больших слоях, пауза при открытом окне.
- prefers-reduced-motion, видимый focus (teal), hover только на устройствах с мышью.

## index.html
- Тема применяется до первой отрисовки (нет вспышки), theme-color меняется с темой.
- Иконки SVG вместо символов и эмодзи. Убраны инлайн-стили и meta no-cache.
- Удалена мёртвая кнопка `#backToNewsBtn` (была скрыта навсегда).

## app.js (39 патчей)
- Тосты, понятные ошибки (`friendlyError`), набор иконок (`ico`).
- Тема: `html.dark`, следует системной, пока пользователь не выбрал сам.
- Кнопка/жест «назад» на телефоне закрывает окно (history).
- Новости: скелетоны, без console.warn, не перерисовываются при повторном открытии, картинки ищутся только для видимых карточек.
- Статья: без инлайн-!important, скелетон, состояние ошибки.
- Калькулятор: состояние загрузки кнопки, прокрутка к результату, исправлена утечка обработчика, SVG вместо эмодзи.
- Чат: пустое состояние с подсказками, один пузырь ошибки, защита от двойной отправки.
- ТН ВЭД: загрузка на кнопке + скелетон результата.
- Справочник: data-label для карточек на телефоне, пустое состояние.
- В списке экспедиторов нет дублей компаний; подсказки городов закрываются по клику снаружи.
- «AI-ассистент» → «ИИ-ассистент» везде.
- Удалён мёртвый код (dock).

## Не сделано
- Стриминг чата: `/api/ai` отдаёт готовый текст, нужна правка server.js (SSE).
- Кнопка «Ставки» открывает импорт экспедиторов — название не совпадает с действием.
