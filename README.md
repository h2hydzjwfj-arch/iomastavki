# Logistics calculator

Render-ready Express app.

## Start
`npm start`

## Login
Default password: `123UUn456TT78*`

## Optional weather
Set Render environment variable `OPENWEATHER_API_KEY`. The key is kept server-side. Without it the animated background still works.
